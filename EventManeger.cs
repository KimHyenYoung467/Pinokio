using UnityEngine;

// 클릭시 화면 전환 
// 클릭 시 오브젝트 상호작용 
// 
// UI - static : Sceen(MultySceen) 



public class EventManeger : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
